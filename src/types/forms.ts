export interface EmailPasswordFields {
  email: string;
  password: string;
}

export interface FilterFields {
  location: string;
  // Range in meters
  // range: number;
  // gender: string;
  // sortBy: string;
}
